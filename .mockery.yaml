with-expecter: true
packages:
  pan/internal:
    config:
      all: true
      dir: mocks/{{ replaceAll .PackagePath "internal" "internal_" }}
      recursive: true
      with-expecter: true
  pan/pkg:
    config:
      all: true
      recursive: true
      with-expecter: true
